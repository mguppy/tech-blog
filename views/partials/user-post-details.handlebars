<div class="card">
{{!-- <a href="/editPost/{{id}}"></a> --}}
  <h5 class="card-header">{{title}}</h5>
  <div class="card-body">
    <p class="card-text">{{content}}</p>
    <button onclick = "location.href = '/editPost/{{id}}';" type="add" value = {{id}} class="btn btn-primary addComment"><i class="fas fa-edit"></i> Add/Edit Comment</button>
  </div>
  <div class="card-footer text-muted">
    Posted on {{ createdAt}}
    {{comment}}
  </div>
</a>
</div>